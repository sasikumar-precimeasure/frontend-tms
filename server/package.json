{
  "name": "modbus-gateway",
  "private": true,
  "version": "0.0.0",
  "type": "commonjs",
  "description": "Local gateway service that owns the real TCP socket to Modbus TCP hardware, ported from ModbusClient.vb using Node's net.Socket. Runs on a machine with network access to the hardware; the tms website talks to it over HTTP.",
  "scripts": {
    "dev": "tsx watch src/server.ts",
    "build": "tsc -p tsconfig.json",
    "start": "node dist/server.js",
    "type-check": "tsc --noEmit"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.21.2"
  },
  "devDependencies": {
    "@types/cors": "^2.8.17",
    "@types/express": "^4.17.21",
    "@types/node": "^24.10.1",
    "tsx": "^4.19.2",
    "typescript": "~5.9.3"
  }
}
